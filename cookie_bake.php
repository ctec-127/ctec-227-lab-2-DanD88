<?php

    // $cookie_name = "user";
    // $cookie_value = "John Doe";
    // setcookie($cookie_name, $cookie_value, time() + (86400 * 30), "/");

        // setcookie('username', $username, time()+ (7200));
        
        // setcookie('firstname', $firstname, time() + (7200));

        // setcookie('lastname', $lastname, time() + (7200));

        setcookie('username', 'BettyW', time() + (7200));

        setcookie('firstname', 'Betty', time() + 7200 ); 

        setcookie('lastname', 'White', time() + (7200));

        // Set 3 cookie array

        setcookie("cookie[institution]", "Clark", time() + (7200));

        setcookie("cookie[city]", "Vancouver", time() + (7200));

        setcookie("cookie[state]", "WA", time() + (7200));

    // setcookie("username", $_POST['username'], time() + (7200));
    // $_COOKIE['username'] = $_POST['BettyW'];

    // setcookie("firstname", $_POST['firstname'], time() + (7200));
    // $_COOKIE['firstname'] = $_POST['Betty'];

    // setcookie("lastname", $_POST['lastname'], time() + (7200));
    // $_COOKIE['lastname'] = $_POST['White'];
    


?>